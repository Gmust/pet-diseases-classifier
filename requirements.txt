# ── Runtime (API server + inference) ──────────────────────────────────────
# This file is used by the Docker/Lambda image.
# Training-only deps (scikit-learn, datasets, etc.) live in requirements-train.txt

fastapi==0.116.1
uvicorn[standard]==0.35.0
mangum==0.19.0
python-dotenv==1.1.1
google-genai==1.35.0

numpy==1.26.4
pandas==2.2.2
pyarrow==16.1.0

torch==2.4.1
transformers==4.44.2
accelerate==0.34.2
