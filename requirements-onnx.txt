# ── Runtime (ONNX inference) ───────────────────────────────────────────────
# Used by Dockerfile.lambda.onnx. Deliberately EXCLUDES torch — that's the whole
# point: ONNX Runtime serves the quantized model, so the heavy torch CPU wheel
# (and accelerate, pandas, pyarrow) drop out of the image.
#
# Produce the model dir first:  python -m app.ml.export_onnx
# Deploy with env: MODEL_BACKEND=onnx  MODEL_PATH=models/transformer_model_onnx

fastapi==0.116.1
uvicorn[standard]==0.35.0
mangum==0.19.0
python-dotenv==1.1.1
google-genai==1.35.0

numpy==1.26.4

# Inference: raw ONNX Runtime + the fast tokenizer. NO torch, NO transformers,
# NO optimum — those would drag torch back into the image and defeat the point.
onnxruntime==1.19.2
tokenizers==0.19.1
